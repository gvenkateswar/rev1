# Optional: guru/student separation (speaker diarization).
#
#   pip install -r requirements-speakers.txt
#
# Resemblyzer pulls in torch and webrtcvad. webrtcvad ships no wheels for
# Python 3.13+ and needs a C compiler (on macOS: xcode-select --install) to
# build from source. If this install fails or you skip it, everything else
# still works — segments just carry no Guru/Student labels, and the run's
# output says so.
resemblyzer>=0.1.1            # speaker embeddings (torch, webrtcvad)
scikit-learn>=1.2.0           # clustering + silhouette auto speaker count

# Best-quality alternative backend (needs a free HF token and a one-time
# license acceptance at hf.co/pyannote/speaker-diarization-3.1):
# pyannote.audio>=3.1.0
