# Core analysis deps. Install with:  pip install -r requirements.txt
# Also requires ffmpeg and yt-dlp on your PATH (install separately).
numpy>=1.24
librosa>=0.10
soundfile>=0.12
scenedetect[opencv]>=0.6.4
opencv-python-headless>=4.8
tqdm>=4.65
