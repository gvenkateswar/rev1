"""Historical video asset pipeline.

A local command-line tool for producing South Asian history YouTube
videos. Stage 1 (shotlist generation) is the only stage implemented so
far; further stages are added incrementally.
"""

__version__ = "0.1.0"
